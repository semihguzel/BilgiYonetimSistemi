@model IEnumerable<BilgiYonetimSistemi.DATA.OgrenimSekli>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.OgrenimTipi)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.OgrenimTipi)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.OgrenimID }) |
            @Html.ActionLink("Details", "Details", new { id=item.OgrenimID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.OgrenimID })
        </td>
    </tr>
}

</table>
