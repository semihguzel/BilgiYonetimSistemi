@model IEnumerable<BilgiYonetimSistemi.DATA.Sinav>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SinavTipi)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SinavTipi)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SinavID }) |
            @Html.ActionLink("Details", "Details", new { id=item.SinavID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SinavID })
        </td>
    </tr>
}

</table>
