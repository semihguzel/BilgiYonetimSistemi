@model IEnumerable<BilgiYonetimSistemi.DATA.Donem>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DonemYili)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DonemYili)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DonemID }) |
            @Html.ActionLink("Details", "Details", new { id=item.DonemID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DonemID })
        </td>
    </tr>
}

</table>
